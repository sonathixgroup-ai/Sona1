import 'package:flutter/material.dart';

class MonPaysColors {
  // Couleurs de base (Identité Nationale)
  static const Color primaryBlue = Color(0xFF1E3A8A); // Bleu profond
  static const Color goldAccent = Color(0xFFFFC107); // Jaune/Or
  static const Color backgroundLight = Color(0xFFF5F7FA);

  // Couleurs d'Urgence (Personnes Recherchées)
  static const Color emergencyBackground = Color(0xFFFFF5F5);
  static const Color emergencyIcon = Color(0xFFE53935);
  static const Color emergencyButton = Color(0xFFD32F2F);
  static const Color emergencyText = Color(0xFFB71C1C);

  // Couleurs des tags (À la Une)
  static const Color tagOfficial = Color(0xFF0D47A1);
  static const Color tagCommunique = Color(0xFF1976D2);
  static const Color tagNational = Color(0xFF00897B);
}
