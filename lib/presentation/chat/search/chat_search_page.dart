import 'package:flutter/material.dart';

class ChatSearchPage extends StatelessWidget {
  const ChatSearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Recherche de chats')),
      body: const Center(child: Text('Page de recherche (à implémenter)')),
    );
  }
}
