// presentation/chat/scheduled_recurring/recurring_schedule_screen.dart
import 'package:flutter/material.dart';

class RecurringScheduleScreen extends StatelessWidget {
  const RecurringScheduleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Programmation récurrente')),
      body: const Center(child: Text('Messages programmés récurrents (à implémenter)')),
    );
  }
}
