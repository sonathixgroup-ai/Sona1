// presentation/chat/smart_notifications/smart_notifications_screen.dart
import 'package:flutter/material.dart';

class SmartNotificationsScreen extends StatelessWidget {
  const SmartNotificationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Notifications intelligentes')),
      body: const Center(child: Text('Paramètres des notifications intelligentes (à implémenter)')),
    );
  }
}
