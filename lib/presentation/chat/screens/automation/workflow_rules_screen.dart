import 'package:flutter/material.dart';

class WorkflowRulesScreen extends StatelessWidget {
  const WorkflowRulesScreen({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Workflows (bêta)')));
}
