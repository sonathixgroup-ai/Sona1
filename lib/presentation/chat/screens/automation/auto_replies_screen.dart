import 'package:flutter/material.dart';

class AutoRepliesScreen extends StatelessWidget {
  const AutoRepliesScreen({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Réponses automatiques (bêta)')));
}
