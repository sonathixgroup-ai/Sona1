import 'package:flutter/material.dart';

class ChatFiltersPage extends StatelessWidget {
  const ChatFiltersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Filtres du chat')),
      body: const Center(child: Text('Filtres (à implémenter)')),
    );
  }
}
